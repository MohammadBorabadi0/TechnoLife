{
    "license": "ISC",
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    },
    "dependencies": {
        "dotenv": "^16.4.5",
        "mongoose": "^8.8.1"
    }
}
